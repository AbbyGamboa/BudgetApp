function Headers({title}){
    return (
        <div className = "d-flex flex-column justify-content-end rounded background-blue m-4 p-3">
            <h1 className="mt-auto mb-0">{title}</h1>
        </div>
    );
}

export default Headers;